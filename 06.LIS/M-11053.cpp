#include <bits/stdc++.h>
using namespace std;
int n,m,ret;

int main(){
    ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
    cin >> n >> m;

    cout << ret << '\n';

    return 0;
}