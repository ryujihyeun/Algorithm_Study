#include <bits/stdc++.h>
using namespace std;
int r,c,m,ret;
int a[101][101];

int main(){
    ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
    cin >> r >> c >> m;

    for(int i = 0; i < m; i++){
        int sr,sc,ss,sd,sz;
        cin >> sr >> sc >> ss >> sd >> sz;
    }

    cout << ret << '\n';

    return 0;
}